/**
 * </h1> Architettura dei Package - IMPLEMENTAZIONE DAO</h1>
 * <p>Questo package contiene le classi concrete che implementano le interfacce definite nel package {@code dao}.</p>
 * * <h2>Funzionalità</h2>
 * <p>Le classi presenti in questo package (es. {@code LezionePostgresDAO}, {@code UtentePostgresDAO})
 * contengono le query SQL effettive per l'interazione con il database PostgreSQL.
 * Si occupano di tradurre i dati estratti dai {@code ResultSet} relazionali in oggetti Java del dominio (Model).</p>
 */
package implementazionedao;